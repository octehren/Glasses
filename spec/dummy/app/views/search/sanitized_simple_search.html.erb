<%= form_for(:sanitized_search_params, url: simple_search_path, method: "get", id: "simple_search_form") do |f| %>

	<%= f.label :first_name, "First Name:" %>
	<%= f.text_field :first_name %>

	<%= f.submit "Search" %>
<% end %>

<% if @players.empty? %>
	<p id="fail">WOPS, NOTHING TO SEARCH</p>
<% else %>

	<% @players.each do |p| %>
		<% @num_results += 1 %>
		<p id="success"><%= p.first_name %>, GR8 SUCCESS</p>
	<% end %>
		<p><%= "#{@num_results}"%></p>
<% end %>